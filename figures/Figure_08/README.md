# Figure 8

Run `python figure_08.py` from this directory. The script reads only the colocated CSV file(s) and writes SVG and 600-dpi JPG files to `output/`. Each CSV carries stable source identifiers, exact human-readable locators, and the scientific boundary applied in the manuscript.
